// Направете програма, в която дефинирайте макро - функция, която приема два аргумента (A и B) и да намира колко е резултата от (A + B) ^ 2. Демонстрирайте използването на това макро в main функцията. Изпълнете стъпката на препроцесинг за тази програма и качете резултата от тази стъпка в отделен файл към заданието.

#include<stdio.h>

#define POWER2(A, B) (A + B)*(A + B)

int main()
{
    int a = 2, b = 3;
    int result;

    result = POWER2(a, b);
    printf("%d", result);

    return 0;

}